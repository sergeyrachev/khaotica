MPEG_transport_stream() {
    do {
        transport_packet()
    } while (nextbits() == 0x47)
}

my_packet(){
     sync_byte                                               128 bslbf

}

payload(){
    for (i=__position(sync_byte);i<184 + __position(sync_byte) ;i++){
        fakerest  8 uimsbf
    }
}

